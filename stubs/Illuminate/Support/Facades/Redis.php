<?php

namespace Illuminate\Support\Facades;

/**
 * IDE stub for Redis facade macros.
 *
 * Register PhpStorm stub root: Settings → PHP → include the project `stubs/` directory
 * (or mark `stubs/` as a Stub content root in Project Structure).
 *
 * @see \App\Providers\v1\RedisMacroServiceProvider::boot()
 *
 * @method static bool setnxex(string $key, string $value, int $ttlSeconds) Atomically SET key value NX EX ttl. Returns true if the key was created, false if it already exists.
 */
class Redis extends Facade {}
